# Flexible I/O (Fio) in Docker Container

Fio is an I/O tool used for workload generation, benchmarking and stress/hardware verification. Now is available using `docker run`.


## Usage

```bash
docker run --rm ljishen/perf --help
```
